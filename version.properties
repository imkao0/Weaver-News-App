# Version source of truth, read by app/build.gradle and auto-bumped by the
# fastlane `deploy_internal` lane.
# VERSION_CODE: monotonically increasing integer (Play requires each upload
# to be higher than the previous).
# VERSION_NAME: CalVer-style YEAR.MONTH.COUNT, bumped by the deploy lane.
VERSION_CODE=1
VERSION_NAME=2026.9.1
